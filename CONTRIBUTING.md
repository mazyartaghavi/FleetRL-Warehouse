# Contributing

1. Create a branch.
2. Add or update tests.
3. Run `python -m pytest -v`.
4. Run `ruff check src tests`.
5. Open a pull request describing the algorithmic or robotics change.

Keep safety supervision independent from learned policies. Do not commit generated models, ROS build folders, or experiment caches.
